{% extends 'basef.html.twig' %}

       {% block Blogs %}
           {% for p in posts %}
                <div class="row pt-md-3">
                    <div class="col-lg-9 col-md-10 mx-auto">
                        <div class="accordion" id="accordionExample">
                            <div class="subject-card mt-4">
                                <div class="subject-card-header p-0" id="headingOne">
                                    <p href="#" class="card__title p-3 p-lg-4 collapsed d-block" data-toggle="collapse" data-target="#collapseOne" aria-expanded="false"
                                       aria-controls="collapseOne">
                                        <div class="row">
                                            <div class="col-md-5 subject-img">
                                                <a href="{{ path('posts_show1', {'id': p.id}) }}"><img src="{{p.imgpost }}"style="height:250px;width:330px;" alt=""></a>
                                            </div>
                                            <div class="col-md-7 subject-content mt-md-0 mt-4">
                                                <h3>{{ p.idPoster }}</h3>
                                                <p>{{ p.contenup }}</p>
                                                    <span class="course-number">
                                                        <span class="fa fa-comments-o" aria-hidden="true"> {{ p.nbcmt }}</span>
                                                        <span class="fa fa-eye" aria-hidden="true"> {{ p.views }}</span>
                                                        <span class="fa fa-star" aria-hidden="true"> {{ p.avis }}</span>
                                                    </span>

                                                <span class="date"aria-hidden="true">
								<label  rel="bookmark" class="entry-date" style="color: var(--title-color);">
									{{ p.dateP | date }}
								</label>
							</span>

                                                {#   {% include 'postsF/rating.html.twig ' with{'rating' 😛.AvgRating } %} #}
                                            </div>

                                        </div>
                                        {#  <div class="checkbox checkbox-secondary">

 <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit() href="{{ path('posts_indexf') }}">Like</button>
                               #</div>
                               <div class="checkbox checkbox-secondary">

                                   <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit() href="{{ path('posts_indexf') }}">Report</button>
                               </div>#}
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}







                                   {% block Blogsup %}
                                   <div class="content">
                                       <div class="container">
                                           <div class="row align-items-center">
                                               <div class="col-lg-6 content-left">
                                                   <h3 style="color:#d45959">Ensemble,</h3>
                                                   <h3 style="color:#d45959">nous y arriverons!</h3>
                                                   <p class="mt-3 mb-lg-5 mb-4">Partagez avec nous vos souvenirs dans nos évenements !</p>

                                                       <a href="{{ path('posts_new')}}" class="btnSingel">Ajouter une publication</a>
                                                                      {# SORT BY DATE#}
                                                   <a class="btnSingel" href="{{ path('posts_indexfdescending') }}">Tendances</a>
                                                                      {# END DATEEEE #}
                                               </div>




                                           </div>
                                           <div class="clear"></div>
                                       </div>
                                   </div>

                               </div>
                               {% endblock %}





    {#    <div class="=vote">
            <div class="vote_bar">
                <div class="vote_progress"></div>
            </div>
           <div class="vote_btns">
               <div class="vote_btn vote_like">145</div>
               <div class="vote_btn vote_dislike">2</div>

           </div>

        </div>
                <div>Like/Dislike</div>#}
{% endblock %}
