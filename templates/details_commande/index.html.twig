{% extends 'base.html.twig' %}

{% block body %}
    <h1>DetailsCommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numc</th>
                <th>Qteproduit</th>
                <th>Prixproduit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for details_commande in details_commandes %}
            <tr>
                <td>{{ details_commande.id }}</td>
                <td>{{ details_commande.numc }}</td>
                <td>{{ details_commande.qteproduit }}</td>
                <td>{{ details_commande.prixproduit }}</td>
                <td>
                    <a href="{{ path('details_commande_show', {'id': details_commande.id}) }}">show</a>
                    <a href="{{ path('details_commande_edit', {'id': details_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('details_commande_new') }}">Create new</a>
{% endblock %}
